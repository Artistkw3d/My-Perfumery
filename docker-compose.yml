services:
  perfume-vault:
    build: .
    ports:
      - "8585:8000"
    volumes:
      - perfume_data:/app/database
    restart: unless-stopped
    environment:
      - FLASK_ENV=production

volumes:
  perfume_data:
