services:
  perfume-vault:
    build: .
    expose:
      - "8000"
    volumes:
      - perfume_data:/app/database
    restart: unless-stopped
    environment:
      - FLASK_ENV=production

volumes:
  perfume_data:
